/**
 * Temporary “site under construction” mode.
 *
 * Revert: set to `false`, then remove the middleware file and the
 * `src/app/maintenance/` route if you no longer need them.
 */
export const MAINTENANCE_MODE = true;
